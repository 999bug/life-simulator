{
  "name": "life-simulator-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Workers + KV 后端：匿名排行榜与云存档接口",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
