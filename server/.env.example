# Cloudflare Workers 本地调试环境变量示例。
# 本项目当前没有必须的服务端密钥，排行榜是公开只读 + 匿名上报接口。
#
# 如果以后要加管理/清理接口，再用以下命令把密钥写入 Worker（不会进代码库）：
#   cd server
#   npx wrangler secret put ADMIN_TOKEN
#
# 复制本文件为 server/.dev.vars 后填入本地值（该文件已被 .gitignore 忽略）：
# ADMIN_TOKEN=replace-me
